streamlit>=1.50.0
Authlib>=1.3.2,<1.4
pandas>=2.0.0
plotly>=5.18.0
numpy>=1.26.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9

# ── Streamlit/분석 런타임 보안 하한 ─────────────────────────────────────────────
GitPython>=3.1.47
Pillow>=11.3.0
pyarrow>=21.0.0
Pygments>=2.20.0

# ── API 연동 ──────────────────────────────────────────────────────────────────
requests>=2.31.0          # data.go.kr HTTP 요청
python-dotenv>=1.0.0      # .env 파일에서 환경변수 로드 (API 키 관리)

# ── 테스트 ────────────────────────────────────────────────────────────────────
pytest>=8.0.0             # 테스트 프레임워크
pytest-mock>=3.12.0       # unittest.mock 래퍼 (mocker fixture)
