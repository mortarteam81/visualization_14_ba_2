"""
data.go.kr API 엔드포인트 상수 및 컬럼 매핑 정의

각 지표별 API URL과 응답 컬럼명 → 내부 표준 컬럼명 매핑을 이 파일에서 중앙 관리합니다.

TODO: data.go.kr에서 API를 신청하고 아래 URL 및 컬럼 매핑을 실제 값으로 수정하세요.
      API 조회 경로: data.go.kr > 데이터찾기 > '대학알리미' 검색
"""

# ── 전임교원 확보율 ───────────────────────────────────────────────────────────
# data.go.kr > 대학알리미 > 전임교원확보현황 API 참조
# TODO: 아래 URL을 data.go.kr에서 발급받은 실제 엔드포인트로 교체하세요.
#       예시 형태: "https://api.odcloud.kr/api/{서비스ID}/v1/uddi:{UUID}"
GYOWON_ENDPOINT = ""

# API 응답 컬럼명 → 내부 표준 컬럼명 매핑
# - 왼쪽(key): API JSON 응답에서 실제로 반환되는 컬럼명
# - 오른쪽(value): 앱 내부에서 사용하는 표준 컬럼명 (CSV 원본 기준)
# TODO: API 응답을 확인한 후 실제 컬럼명으로 수정하세요.
#       확인 방법: DataGoKrClient.get() 호출 후 응답 keys() 출력
GYOWON_COLUMN_MAP: dict[str, str] = {
    # API 응답 컬럼명          : 내부 표준 컬럼명
    "기준년도"                : "기준년도",
    "학교명"                  : "학교명",
    "본분교구분"               : "본분교명",     # CSV의 '본분교명' 컬럼과 매핑
    "설립유형구분"             : "설립유형",     # CSV의 '설립유형' 컬럼과 매핑
    # 전임교원 확보율 컬럼명은 API 응답 확인 후 수정 필요
    "전임교원확보율(학생정원기준)(%)": "전임교원 확보율(학생정원 기준)",
    "전임교원확보율(재학생기준)(%)":   "전임교원 확보율(재학생 기준)",
}
